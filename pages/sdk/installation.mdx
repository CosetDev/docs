# Installation

Start by installing *@coset-dev/sdk* using npm.

```bash
npm install @coset-dev/sdk
```

Alternatively, you can install using pnpm

```bash
pnpm add @coset-dev/sdk
```

Now you are ready to start.
